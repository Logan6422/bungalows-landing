---
---
<section class="map-section" id="ubicacion">
  <div class="map-text">
    <div class="map-text-inner">
      <span class="map-label">¿Dónde estamos?</span>
      <h3>Ubicación</h3>
      <p>
        Ubicados en San Javier, a pocos metros del río, en un entorno natural
        ideal para descansar y desconectar.
      </p>
      <a
        href="https://maps.app.goo.gl/kgyahSBEcUTpHr817"
        target="_blank"
        class="map-cta"
      >
        Ver en Google Maps ↗
      </a>
    </div>
  </div>

  <div class="map-container">
    <iframe
      src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3434.647151985502!2d-59.934507474697845!3d-30.58750469649034!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x944b573678a985cb%3A0x5ad40ad2fd6f6faa!2sBungalows%20El%20Cuaco!5e0!3m2!1ses-419!2sar!4v1772321824659!5m2!1ses-419!2sar"
      width="100%"
      height="100%"
      style="border:0;"
      allowfullscreen=""
      loading="lazy"
      referrerpolicy="no-referrer-when-downgrade">
    </iframe>
  </div>
</section>

<style>
  .map-section {
    background: #3E2A1F;
    padding: 7rem 6vw;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 5rem;
  }

  /* TEXT */
  .map-text {
    flex: 0 0 380px;
    color: #F5F3EE;
  }

  .map-text-inner {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .map-label {
    font-size: 0.85rem;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: #a0856e;
    font-weight: 500;
  }

  .map-text h3 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2rem, 3vw, 3rem);
    font-weight: 400;
    line-height: 1.2;
    margin: 0;
  }

  .map-text p {
    font-size: 1.1rem;
    line-height: 1.9rem;
    opacity: 0.85;
    margin: 0;
  }

  .map-cta {
    display: inline-block;
    margin-top: 0.5rem;
    padding: 0.7rem 1.6rem;
    background: transparent;
    border: 1.5px solid rgba(245, 243, 238, 0.4);
    border-radius: 40px;
    color: #F5F3EE;
    font-size: 0.95rem;
    text-decoration: none;
    transition: all 0.3s ease;
    width: fit-content;
  }

  .map-cta:hover {
    background: #F5F3EE;
    color: #3E2A1F;
    border-color: #F5F3EE;
  }

  /* MAP */
  .map-container {
    flex: 0 0 520px;
    height: 380px;
    border-radius: 18px;
    overflow: hidden;
    box-shadow: 0 25px 60px rgba(0,0,0,0.35);
    transition: transform 0.5s ease, box-shadow 0.5s ease;
  }

  .map-container:hover {
    transform: translateY(-5px);
    box-shadow: 0 35px 80px rgba(0,0,0,0.45);
  }

  .map-container iframe {
    width: 100%;
    height: 100%;
    filter: grayscale(20%) contrast(95%);
  }

  /* RESPONSIVE */
  @media (max-width: 1000px) {
    .map-section {
      flex-direction: column;
      gap: 2.5rem;
      padding: 5rem 5vw;
    }

    .map-text {
      flex: unset;
      width: 100%;
      max-width: 560px;
      text-align: center;
    }

    .map-text-inner {
      align-items: center;
    }

    .map-container {
      flex: unset;
      width: 100%;
      max-width: 560px;
      height: 280px;
    }
  }

  @media (max-width: 480px) {
    .map-section {
      padding: 4rem 1.5rem;
    }

    .map-container {
      height: 240px;
      border-radius: 14px;
    }

    .map-text h3 {
      font-size: 2rem;
    }
  }
</style>